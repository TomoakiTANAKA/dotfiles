[include]
  path = ~/.gitconfig.d/.gitconfig.local
  path = ~/.gitconfig.d/.gitconfig.common
[core]
  editor = vim -c \"set fenc=utf-8\"
[color]
  ui = auto
  diff = auto
  status = auto
  branch = auto
